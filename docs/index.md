---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "noozle Provider"
description: |-
  Manages Noozle queries and delivery outlets through the Noozle service and API documentation https://noozle.io.
---

# noozle Provider

Manages Noozle queries and delivery outlets through the [Noozle service and API documentation](https://noozle.io).

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

provider "noozle" {
  host            = "https://api.noozle.io"
  api_key         = "nk2_replace_me"
  tenant_id       = "t_replace_me"
  organization_id = "org_replace_me"
  debug           = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Noozle API key. Can also be set with `NOOZLE_API_KEY`.
- `debug` (Boolean) Enable debug logging for HTTP request and response metadata. Payload contents are never logged.
- `host` (String) Noozle API base URL. Can also be set with `NOOZLE_HOST`.
- `organization_id` (String) Optional organization context used to disambiguate the selected tenant. Can also be set with `NOOZLE_ORGANIZATION_ID`.
- `tenant_id` (String) Required Tenant context for query, outlet, and outlet-query-link operations. Set `tenant_id` or `NOOZLE_TENANT_ID`.
